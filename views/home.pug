extends layouts/main.pug

include mixins/videoBlock
block content
    .home-videos
        each video in videos
           +videoBlock({
               id:video._id,
               title: video.title,
               view: video.view,
               fileUrl: video.fileUrl
           })