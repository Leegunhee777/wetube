extends layouts/main.pug

include mixins/videoBlock
block content
    .videos
        h1 Video
        p #{pageTitle}
        each video in videos
           +videoBlock({
               id:video.id,
               title: video.title,
               views: video.views,
               videoFile: video.videoFile
           })